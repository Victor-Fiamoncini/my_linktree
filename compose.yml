services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: my_linktree
      POSTGRES_PASSWORD: my_linktree
      POSTGRES_DB: my_linktree_local
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
